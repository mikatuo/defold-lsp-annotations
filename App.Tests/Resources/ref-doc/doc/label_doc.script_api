- desc: The color of the label. The type of the property is vector4.
  name: color
  type: property
- desc: The outline color of the label. The type of the property is vector4.
  name: outline
  type: property
- desc: The shadow color of the label. The type of the property is vector4.
  name: shadow
  type: property
- desc: 'The scale of the label. The type of the property is number (uniform)

    or vector3 (non uniform).'
  name: scale
  type: property
- desc: 'Returns the size of the label. The size will constrain the text if line break
    is enabled.

    The type of the property is vector3.'
  name: size
  type: property
- desc: The material used when rendering the label. The type of the property is hash.
  name: material
  type: property
- desc: The font used when rendering the label. The type of the property is hash.
  name: font
  type: property
- desc: ''
  members:
  - desc: 'Sets the text of a label component


      [icon:attention] This method uses the message passing that means the value will
      be set after `dispatch messages` step.

      More information is available in the <a href="/manuals/application-lifecycle">Application
      Lifecycle manual</a>.'
    name: set_text
    parameters:
    - desc: the label that should have a constant set
      name: url
      type:
      - string
      - hash
      - url
    - desc: the text
      name: text
      type: string
    type: function
  - desc: Gets the text from a label component
    name: get_text
    parameters:
    - desc: the label to get the text from
      name: url
      type:
      - string
      - hash
      - url
    returns:
    - desc: the label text
      type: string
    type: function
  name: label
  type: table
